#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character.
 * @c: a character to check
 *
 * Return: 0 or 1
 */
int _isupper(int c)
{
	char c;

	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}

